$LOAD_PATH.unshift File.expand_path("../lib", __FILE__)
require "pc/version"
 
task :build do
    system "gem build .gemspec"
end
 
task :release => :build do
    system "gem push pc-#{Bunder::VERSION}"
end
